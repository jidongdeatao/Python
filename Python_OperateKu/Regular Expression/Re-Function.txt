常见的正则表达式函数有
  1.  re.match()函数
  2.  re.search()函数
  3.  全局匹配函数
  4.  re.sub()函数
  
1.  re.match()函数
  使用格式：re.match(pattern,string,flag)
    解释： 第一个参数代表对应的正则表达式，第二个参数代表对应的源字符，第三个参数是可选参数，代表对应的标志位，可以放模式修正符等信息。
    
