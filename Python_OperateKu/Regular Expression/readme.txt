正则表达式

目录：
  1.特殊符号和字符
    1）使用择一匹配符号”|“匹配多个正则表达式模式
    2）匹配任意单个字符(.)
    3）从字符串起始(^)或者结尾($)或者边界匹配(\b \B)
    4）创建字符集([])
    5）限定范围([0-9]、[a-z]等）和否定(^紧跟在[之后，[^])
    6）使用闭包操作符实现存在性和频数匹配（*匹配左边出现零次或多次情况，+匹配一次或多次，？匹配零次或一次,{N}匹配N次，{M,N}匹配M到N次）
    7）表示字符集的特殊字符（\d匹配任何十进制数字，\w表示全部字母数字的字符集，\s表示空格，等）
    8）使用圆括号指定分组
    9）扩展表示法（以问号开始(?P<name>)
  2.正则表达式和python语言
    1）re模块：核心参数和方法
    2）使用compile()函数编译正则表达式
    3）匹配对象以及group()和groups()方法
    4）使用match()方法匹配字符串
    5）使用search()在一个字符串中查找模式（搜索与匹配的对比）
    6）匹配多个字符串
    7）匹配任何单个字符（.）
    8）创建字符集（[]）
    9）重复、特殊字符以及分组
    10）匹配字符串的起始和结尾以及单词边界
    11）使用findall()和finditer()查找每一次出现的位置
    12）使用sub()和subn()搜索与替换
    13）在限定模式上使用split()分隔字符串
    14）扩展符号(?P<name>)与(?P=name)
    15）杂项
  3.一下正则表达式示例
    参见example.txt
    
  4.python正则表达式常见函数
    使用re模块
